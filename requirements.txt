# 🎊 微信拜年自动回复脚本依赖

# 核心依赖
pyautogui>=0.9.54      # Windows: 窗口控制、模拟输入
pyperclip>=1.8.2       # 跨平台: 剪贴板操作
pyyaml>=6.0            # 配置文件解析
python-dotenv>=1.0.0    # 环境变量

# macOS 专用 (可选)
# pyobjc>=9.0           # macOS 原生支持 (如需更深入的 macOS 自动化)
